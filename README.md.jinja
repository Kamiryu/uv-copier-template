# {{ project_name }}

{{ description }}

## Setup (uv)

```bash
uv sync
```

Create a local environment file:

```bash
cp .env.example .env
```

Install only specific groups:

```bash
uv sync --group dev --group lint --group test
```

## Configuration

Settings are loaded from environment variables and `.env` using
[`pydantic-settings`](https://docs.pydantic.dev/latest/concepts/pydantic_settings/).

```python
from {{ package_name }}.settings import settings

print(settings.environment)
```

## Common commands

Run tests:

```bash
uv run pytest
```

Lint + format:

```bash
uv run ruff check .
uv run ruff format .
```

Type check:

```bash
uv run mypy src
```

{% if project_type == "cli" %}
## CLI

Run the CLI:

```bash
uv run {{ project_slug }} --help
```
{% endif %}

{% if project_type == "api" %}
## API

Run the API:

```bash
uv run uvicorn {{ package_name }}.api:app --reload
```

Healthcheck:

- `GET /health`
{% endif %}
